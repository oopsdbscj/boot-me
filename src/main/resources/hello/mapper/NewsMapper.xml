<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.mapper.NewsMapper">
    <!--<resultMap type="org.apache.ibatis.submitted.rounding.User" id="usermap">-->
        <!--<id column="id" property="id"/>-->
        <!--<result column="name" property="name"/>-->
        <!--<result column="funkyNumber" property="funkyNumber"/>-->
        <!--<result column="roundingMode" property="roundingMode"/>-->
    <!--</resultMap>-->

    <select id="getNews" resultType="News">
        select * from t_news WHERE id = #{id}
    </select>
    <select id="firstClass" resultType="News">
        select * from t_news order by id DESC limit 0,10;
    </select>
    <select id="getMap" resultMap="idMap">
        select n.id as idd,n.* from t_news n WHERE id =200
    </select>
    <resultMap type="hashmap" id="idMap">
        <id column="id" property="id"/>
        <association property="name" javaType="News" columnPrefix="n."/>
    </resultMap>
    <!--<insert id="insert">
        insert into users (id, name, funkyNumber, roundingMode) values (
        #{id}, #{name}, #{funkyNumber}, #{roundingMode}
        )
    </insert>

    <resultMap type="org.apache.ibatis.submitted.rounding.User" id="usermap2">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="funkyNumber" property="funkyNumber"/>
        <result column="roundingMode" property="roundingMode"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
    </resultMap>
    <select id="getUser2" resultMap="usermap2">
        select * from users2
    </select>
    <insert id="insert2">
        insert into users2 (id, name, funkyNumber, roundingMode) values (
        #{id}, #{name}, #{funkyNumber}, #{roundingMode, typeHandler=org.apache.ibatis.type.EnumTypeHandler}
        )
    </insert>-->

</mapper>
